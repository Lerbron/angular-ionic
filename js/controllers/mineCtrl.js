/** * Created by admin on 2017/6/23. */app.controller('mineCtrl', function ($scope, useService) {	// 定义显示登陆、注册的界面。 true为登陆， false为注册。	$scope.toggleState = true;	// 定义切换登陆注册页面的方法	$scope.toggle = function (state) {		$scope.toggleState = state;	}	$scope.user = {};	$scope.r_user = {};	$scope.login = function (obj) {		obj.status = 'login';		useService.login(obj).then(function (e) {			if ( e == 0) {				alert ('用户名不存在')			} else if ( e == 2) {				alert('用户名密码错误')			} else {				alert('登陆成功');				sessionStorage.setItem('user', JSON.stringify(e))			}		})	}	$scope.regist = function () {		$scope.r_user.status = 'register';		useService.register($scope.r_user).then( function (e) {			if (e == 1 ) {				alert('注册成功');			} else if ( e == 0) {				alert('用户名已存在');			} else {				alert('网络异常')			}		})	}})